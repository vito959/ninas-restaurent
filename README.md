# Nina's Restaurant

Static one-page site for Nina's Restaurant, deployed on Cloudflare Pages.

## Local preview

Open `index.html` directly in a browser, or serve it locally:

```
npx serve .
```

## Deploy

Pushing to `main` triggers an automatic deploy via Cloudflare Pages' Git integration.
